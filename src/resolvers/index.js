const {Query} = require("./Query")
const {User} = require("./User")
const {Product} = require("./Product")
const Mutation = require('./Mutation/Mutation')


module.exports = {Query, User, Mutation, Product}